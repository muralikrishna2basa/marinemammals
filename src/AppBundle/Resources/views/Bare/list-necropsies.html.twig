<h2>List of samples</h2>
<p class="count">
    {{ paginator.getItemscount}} samples found.
</p>
<div class="pagination_head">
    {{ paginator_html|raw }}
</div>

<table class="table table-striped table-samples" style="width:750px;">
    <thead>
    <tr>
        <th>species</th>
        <th>sex</th>
        <th>date found</th>
        <th>necropsy reference</th>
        <th>collection tag</th>
        <th>date necropsied</th>
        <th>select</th>
    </tr>
    </thead>
    {% for s in necropsies %}
        <tr>
            {#<td>{{ n.txn_vernacularNameEn }} (<i>{{ n.txn_canonicalName }}</i>)</td>
            <td>{{ n.scn_sex }}</td>
            <td>{{ n.DateFound|date("d/m/Y") }}</td>
            <td>{{ n. }}</td>
            <td>{{ n.ese_eventDatetime|date("d/m/Y") }}</td>#}
        </tr>
    {% else %}
        <tr>
            <td>No necropsies found.</td>
        </tr>
    {% endfor %}
</table>
<div class="pagination_foot">
    {{ paginator_html|raw }}
</div>