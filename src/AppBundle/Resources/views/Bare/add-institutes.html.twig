<h2>Add new institute</h2>
{{ form_start(iform, {'method': 'POST','action': path('mm_persons_add_create')}) }}
<fieldset>
    <div class="form-inline">{{ form_row(iform.name, {'label':'Name'}) }}</div>
    <div class="form-inline">{{ form_row(iform.code, {'label':'Code'}) }}</div>
</fieldset>
<p>
    <input type="submit" value="Submit">
</p>
{{ form_end(iform) }}